@model List<JohnHenryFashionWeb.Models.PushNotificationCampaign>
@{
    ViewData["Title"] = "Push Notification Campaigns";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<style>
    .marketing-container {
        padding: 2rem;
        background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        min-height: calc(100vh - 80px);
    }

    .page-header-glass, .push-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(15px);
        border-radius: 16px;
        padding: 1.5rem;
        margin-bottom: 1.5rem;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .push-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
    }

    .btn-primary-glass {
        background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        color: white;
        border: none;
        padding: 0.5rem 1.5rem;
        border-radius: 10px;
    }
</style>

<div class="marketing-container">
    <div class="page-header-glass">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h2 class="mb-1">🔔 Push Notification Campaigns</h2>
                <p class="text-muted mb-0">Quản lý chiến dịch push notification</p>
            </div>
            <a href="/admin/marketing/push/create" class="btn btn-primary-glass">
                <i class="fas fa-plus"></i> Tạo Campaign
            </a>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (Model == null || !Model.Any())
    {
        <div class="push-card text-center">
            <p class="text-muted mb-0">Chưa có campaign nào.</p>
        </div>
    }
    else
    {
        @foreach (var push in Model)
        {
            <div class="push-card">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h5 class="mb-1">@push.Title</h5>
                        <p class="text-muted mb-2">@push.Message</p>
                        <div class="d-flex gap-3">
                            <span class="badge bg-@(push.Status == "sent" ? "success" : push.Status == "sending" ? "warning" : "secondary")">
                                @push.Status
                            </span>
                            <span><strong>Audience:</strong> @push.TargetAudience</span>
                            @if (push.SentCount > 0)
                            {
                                <span><strong>Sent:</strong> @push.SentCount / @push.TotalRecipients</span>
                            }
                        </div>
                        @if (push.ScheduledAt.HasValue)
                        {
                            <p class="mt-2 mb-0"><small><i class="fas fa-clock"></i> @push.ScheduledAt.Value.ToString("dd/MM/yyyy HH:mm")</small></p>
                        }
                    </div>
                    <div class="col-md-4 text-end">
                        <div class="d-flex gap-2 justify-content-end">
                            <a href="/admin/marketing/push/@push.Id" class="btn btn-sm btn-outline-primary">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <form method="post" action="/admin/marketing/push/@push.Id/delete" style="display:inline;" onsubmit="return confirm('Delete?');">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-danger">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        }
    }
</div>
